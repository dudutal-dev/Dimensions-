# מצפן המימדים — מסמך תכנון לבנייה ב-Claude Code
**אפליקציית דגל לתודעת 3D / 4D / 5D** · גרסת מסמך 1.1 · 21.9.2026
שם עבודה: **"מצפן המימדים"** (Dimension Compass). ניתן לשינוי.

> **ל-Claude Code:** קרא את המסמך כולו לפני כתיבת קוד. סדר העבודה מחייב (פרק 12). אבן הדרך הראשונה היא **חבילת התוכן בלבד** — הצג אותה לאישור דודו לפני שממשיכים.

---

## 1. חזון ועקרונות

**מה האפליקציה עושה:** מלמדת שתי מיומנויות — **לזהות** באיזה מצב תודעה אני נמצא ברגע נתון, ו**לעבור** למצב הרצוי — ומלווה תהליך של 12 שבועות לייצוב. היא מאמן אישי בכיס, לא ספר דיגיטלי.

**מקור האמת לתוכן:** תיקיית `content-source/` (הסקיל `quantum-dimensions-expert`). אין להמציא תוכן מקצועי שאינו שם; אפשר לערוך ניסוח לקריאה במסך.

**עקרונות מוצר:**
1. **פעולה לפני קריאה.** כל מסך מוביל לתרגול או לזיהוי תוך 2 הקשות. חומר עיוני נמצא בספרייה, לא בדרך.
2. **הנתון החשוב הוא מפת החום האישית** — מתי, איפה ועם מי אני בכל מצב. כל האפליקציה בנויה סביב איסופו והצגתו.
3. **יושר אינטלקטואלי.** "מימד" = מצב תודעה, לא מקום ביקום. נאמר פעם אחת ב-Onboarding ובספרייה; לא מנדנדים בכל מסך.
4. **אין "הגעה".** אין מסך "הגעת ל-5D", אין XP, אין דירוג מול אחרים. מודדים זמן התאוששות ושכיחות, לא "רמה".
5. **רוגע בעיצוב.** האפליקציה עצמה צריכה להרגיש כמו 5D: מרווחת, איטית, בלי התראות אדומות ובלי streak-shaming.
6. **פרטיות מוחלטת.** הכול מקומי במכשיר. אין שרת, אין אנליטיקס, אין חשבון.
7. **עיניים עצומות.** תרגולים רבים נעשים בעיניים עצומות — אזורי מגע ענקיים, צלילי מעבר, ובשלב ב' הדרכה קולית.

**משתמש יעד:** משתמש יחיד (דודו), iPhone + Safari כמכשיר ראשי, גם דסקטופ. עברית RTL. ללא עלויות שוטפות מלבד קריאות API חד-פעמיות בשלב ב'.

---

## 2. החלטות ארכיטקטורה (ARD)

| # | החלטה | נימוק |
|---|---|---|
| ARD-1 | **PWA סטטי**: Vite + React 18 + TypeScript (strict) | אפליקציית דגל רב-מסכית; בנייה מודולרית ב-Claude Code; פלט סטטי ל-GitHub Pages; מותקן למסך הבית באייפון |
| ARD-2 | **Tailwind CSS** + design tokens כ-CSS variables | עקביות, dark/light, RTL לוגי (`ps-`/`pe-`, `start`/`end`) |
| ARD-3 | **Zustand** למצב · **Dexie (IndexedDB)** לנתונים | נתוני יומן מצטברים לאורך חודשים; localStorage קטן מדי ושביר |
| ARD-4 | **תוכן כ-JSON מטיפוסי** ב-`src/content/` עם סכמת Zod | הפרדת תוכן מקוד; ולידציה ב-build; מקל על שלב ב' (תסריטי קול) |
| ARD-5 | **Framer Motion** לאנימציה · **Lucide** לאייקונים · תרשימים ב-SVG ידני | שליטה מלאה במראה; בלי ספריית charts כבדה |
| ARD-6 | **vite-plugin-pwa** (Workbox) — offline מלא | תרגול גם בלי קליטה; אודיו של שלב ב' נשמר ב-cache לפי דרישה |
| ARD-7 | **מנוע אודיו אחד** (`AudioEngine`) לכל הצלילים | לקח מאפליקציית Gateway: באייפון Web Audio שתק בעוד הדיבור נשמע. ראה פרק 9 |
| ARD-8 | **שלב ב': אודיו מופק מראש** בסקריפט Node מקומי, לא בזמן ריצה | המפתח של ElevenLabs לא נחשף באתר ציבורי; עלות חד-פעמית; עובד offline |
| ARD-9 | בדיקות: **Vitest** (לוגיקה) + **Playwright** (ויזואלי, viewport אייפון, RTL) | תואם את ה-stack הקיים של דודו |
| ARD-10 | פריסה: **GitHub Pages** דרך GitHub Actions, `base` יחסי | כמו האפליקציות הקודמות |

**לא בתחום (במכוון):** backend, התחברות, סנכרון ענן, push notifications (דורש שרת; ראה חלופה בפרק 6.8), רב-משתמשים.

---

## 3. מבנה הפרויקט

```
dimension-compass/
├── CLAUDE.md                      # הנחיות קבועות ל-Claude Code (מצורף)
├── SPEC.md                        # המסמך הזה
├── content-source/                # מקור האמת: קבצי הסקיל (קריאה בלבד)
├── public/
│   ├── audio/ui/                  # צלילי ממשק (פעמון, מעבר) — שלב א'
│   ├── audio/voice/{he,en}/…      # הדרכה קולית — שלב ב' (נוצר ע"י הסקריפט)
│   ├── fonts/                     # גופנים מקומיים (woff2)
│   └── icons/                     # אייקוני PWA
├── src/
│   ├── app/                       # Router, Shell, BottomNav, providers
│   ├── design/                    # tokens.css, motion.ts, primitives (Button, Card, Sheet…)
│   ├── content/                   # *.json + schema.ts (Zod) + loaders
│   ├── domain/                    # לוגיקה טהורה, ללא React: scoring, heatmap, journey, language-markers
│   ├── data/                      # Dexie db, repositories, backup (export/import)
│   ├── audio/                     # AudioEngine, AmbientPad, SegmentPlayer (שלב ב')
│   ├── features/
│   │   ├── onboarding/  ├── today/  ├── checkin/  ├── diagnosis/
│   │   ├── shift/       ├── session/  ├── journey/  ├── insights/
│   │   ├── journal/     ├── library/  ├── sos/  └── settings/
│   └── lib/                       # date, rtl, haptics, ics
├── tools/
│   ├── build-content.mjs          # ולידציית content JSON
│   └── voice/                     # שלב ב': generate-voice.mjs, audition.mjs, estimate.mjs
├── voice-scripts/                 # שלב ב': תסריטי הדרכה (JSON)
├── tests/ (unit/, e2e/)
└── .env.example                   # ELEVENLABS_API_KEY= (ה-.env עצמו ב-.gitignore)
```

**כלל:** `domain/` אינו מייבא מ-React או מ-Dexie. כל חישוב ניתן לבדיקה ביחידה.

---

## 4. מערכת העיצוב (לפי ui-ux-pro-max)

**סוג מוצר:** Wellness / Mindfulness, mobile-first. **כיוון:** "Aurora Calm" — כהה כברירת מחדל, עומק רך, הילה איטית ברקע; glass עדין **רק** ל-sheets ולניווט (blur מסמן שכבה, לא קישוט).

### 4.1 צבע — tokens סמנטיים (אין hex גולמי בקומפוננטות)
| Token | Dark | Light | שימוש |
|---|---|---|---|
| `--bg` | `#0B0F1A` | `#F7F5F0` | רקע |
| `--surface` | `#141A2A` | `#FFFFFF` | כרטיסים |
| `--surface-2` | `#1C2438` | `#EFECE5` | כרטיס מורם / input |
| `--text` | `#ECEFF7` | `#1B2030` | טקסט ראשי |
| `--text-muted` | `#A3ABC2` | `#5A6275` | טקסט משני (≥4.5:1 — לבדוק) |
| `--border` | `#2A3350` | `#DDD8CC` | |
| `--d3` | `#E8935A` | `#B85F22` | מצב 3D — ענבר-אדמה (חם, מקורקע) |
| `--d4` | `#8B7CF6` | `#5B48D6` | מצב 4D — אינדיגו-סגול (מעבר, עומק) |
| `--d5` | `#5EDCC8` | `#0F8F7E` | מצב 5D — טורקיז זוהר (מרחב, בהירות) |
| `--accent` | `#F2D492` | `#9A7420` | זהב רך — CTA ראשי, הדגשות |
| `--danger` | `#F08A8A` | `#B3261E` | מחיקה בלבד |

**צבע אינו נושא מידע לבדו:** לכל מצב גם **צורה** ו**תווית** — 3D = קובייה איזומטרית, 4D = שתי טבעות משולבות, 5D = עיגול קורן. SVG ייעודיים ב-`design/DimensionGlyph.tsx`. תמיד מופיע גם הטקסט "3D/4D/5D".

**חשוב — ניטרליות ערכית:** 3D אינו "אדום/רע". הצבע החם מבטא קרקוע. אין צבעי רמזור.

### 4.2 טיפוגרפיה (גופנים מקומיים, `font-display: swap`)
- כותרות: **Frank Ruhl Libre** (500/700) — סריף עברי, מהורהר.
- גוף וממשק: **Heebo** (400/500/600).
- סולם: 13 · 15 · 17 (גוף) · 20 · 26 · 34. גוף ≥17px במובייל. `line-height` 1.65 לגוף. ספרות טבלאיות בטיימרים (`font-variant-numeric: tabular-nums`).
- אורך שורה 35–60 תווים במובייל.

### 4.3 מרחב, צורה, עומק
ריווח על רשת 4pt (4/8/12/16/24/32/48). רדיוס: 12 (פקדים) · 20 (כרטיסים) · 28 (sheets). סולם הצללה אחד בן 3 דרגות. `max-width` תוכן 560px, ממורכז בדסקטופ; מ-1024px — ניווט צד במקום תחתון.

### 4.4 תנועה
- מיקרו-אינטראקציות 180–260ms, ease-out בכניסה; יציאה ~65% מזמן הכניסה; spring רך למעברי מסך.
- **הילת הרקע** ו**עיגול הנשימה** הם היחידים שנעים לאט (שניות) — כי הם *התוכן*.
- `prefers-reduced-motion`: ההילה סטטית; עיגול הנשימה מתחלף ב-progress ליניארי + טקסט ("שאיפה…4").
- מגע: scale 0.97 בלחיצה; משוב חזותי <100ms.

### 4.5 כללי חובה
יעדי מגע ≥48px ומרווח ≥8px · CTA ראשי אחד למסך · `min-h-dvh` ו-`env(safe-area-inset-*)` · ניווט תחתון עם אייקון+תווית, 5 פריטים · focus ring נראה · `aria-live="polite"` ל-toasts · `dir="rtl" lang="he"`; מאפיינים לוגיים בלבד · אין emoji כאייקונים (סימוני 🔬🧪🧘 מהסקיל הופכים לתגיות SVG: "מבוסס" / "ספקולטיבי" / "מטפורי").

לפני מסירת כל אבן דרך: להריץ את ה-Pre-Delivery Checklist של ui-ux-pro-max.

### 4.6 מובייל תחילה — דרישת יסוד
הסלולר הוא הפלטפורמה הראשית; דסקטופ הוא הרחבה. מתכננים ובודקים קודם ב-375px.
- **מכשירי יעד:** iPhone (Safari + PWA מותקן) — ראשי; Android (Chrome + PWA מותקן) — נתמך במלואו; דסקטופ — משני.
- **התקנה למסך הבית:** manifest מלא (שם, אייקונים כולל maskable, `display: standalone`, צבעי theme), `apple-touch-icon` ומסכי splash ל-iOS; מסך הדרכה קצר "איך מתקינים" (שיתוף ← הוסף למסך הבית ב-iOS; תפריט ← התקן ב-Android).
- **שימוש ביד אחת:** כל פעולה ראשית בשליש התחתון של המסך; CTA ראשי צמוד לתחתית מעל הניווט; sheets נפתחים מלמטה ונסגרים בהחלקה.
- **Safe areas:** `viewport-fit=cover` + `env(safe-area-inset-*)` לניווט התחתון, לכפתור הצף ולנגן; `min-h-dvh` (לא `100vh`).
- **מקלדת:** שדות ≥16px (מונע zoom אוטומטי ב-iOS); המסך נגלל כך שהשדה הפעיל נראה מעל המקלדת; `inputmode`/`enterkeyhint` מתאימים.
- **מגע:** `touch-action: manipulation`; בלי תלות ב-hover; בלי החלקה אופקית על תוכן ראשי (מתנגש בחזרה של iOS); רטט עדין (`navigator.vibrate`) ב-Android בלבד — iOS Safari אינו תומך, ולכן לכל רטט יש גם משוב חזותי/צלילי.
- **נגן במסך נעול:** Wake Lock בזמן סשן; Media Session API (כותרת התרגול + השהה/המשך במסך הנעילה) — חשוב במיוחד בשלב ב'.
- **סוללה ונתונים:** הילת הרקע נעצרת כשהלשונית מוסתרת ובמצב חיסכון; אודיו קולי נטען לפי דרישה בלבד.
- **כיוון מסך:** נעול לאורך ב-manifest; פריסה סבירה גם לרוחב.
- **בדיקות:** Playwright עם פרופילי iPhone 14 ו-Pixel 7 + בדיקה ידנית על מכשיר פיזי בכל אבן דרך שנוגעת לאודיו, להתקנה או למקלדת.

---

## 5. ארכיטקטורת מידע וניווט

**ניווט תחתון (5):** `היום` · `בדיקה` · `מעבר` · `מסע` · `תובנות`
**משני (מאייקון פרופיל בראש "היום"):** יומן · ספרייה · הגדרות · גיבוי · "צריך עזרה?"
**כפתור צף קבוע בכל המסכים:** **"90 שניות"** (SOS) — פותח מיד את פרוטוקול המעבר המהיר.

Deep links לכל מסך ראשי (`#/checkin`, `#/shift/heart-drop`, `#/journey/w4`). Hash router — תואם GitHub Pages.

---

## 6. מסכים ופיצ'רים

### 6.1 Onboarding (פעם אחת, 5 מסכים, ניתן לדילוג)
1. ברוכים הבאים — מה האפליקציה עושה (משפט אחד).
2. **המסגור הכן:** "מימד כאן הוא מצב תודעה, לא מקום ביקום. זו מפה להתפתחות אישית; הכלים שבה — נשימה, תיוג, חמלה — נתמכים מחקרית." + קישור "לקריאה נוספת" לספרייה.
3. שלושת המצבים — שלוש הגליפים, משפט לכל אחד, "5D מכיל את 3D ו-4D".
4. גבולות: לא תחליף לטיפול; מה לעשות אם עולה מצוקה.
5. בחירה: "להתחיל באבחון מלא (8 דק')" או "לקפוץ לבדיקה ראשונה (60 שנ')".

### 6.2 היום (בית)
- ברכה לפי שעה + **הילה בצבע המצב האחרון שנרשם** (עדין).
- כרטיס ראשי (CTA יחיד): המשימה הבאה — בדיקת מימד / תרגול היום מהמסע / ערב: יומן.
- "עוגני היום": 5 נקודות (התעוררות · לפני ישיבה ראשונה · אחרי צהריים · כניסה הביתה · לפני שינה) — מתמלאות בגליף של המצב שנרשם.
- תרגול היום (מהמסע) + משימת החיים השבועית.
- שורת תובנה אחת מתחלפת ("השבוע: 3D מופיע בעיקר בימי א' בבוקר, בתחום זמן").

### 6.3 בדיקת מימד — 60 שניות (הפיצ'ר המרכזי)
מקור: `identification.md §7`. חמישה צעדים, מסך לכל צעד, הקשה אחת בכל אחד:
1. **נשימה** — חזה/בטן · עצורה/זורמת
2. **כיווץ** — סליידר 0–10 + צ'יפים (לסת/כתפיים/בטן)
3. **מחשבה אחרונה** — על מה? (צ'יפים: איום, השוואה, תכנון, ניתוח עצמי, שקט…) + "האם האמנתי לה?"
4. **טון רגשי** — גלגל מילים בשלוש טבעות (לא מסומנות כ-3D/4D/5D בפני המשתמש)
5. **זמן** — עבר / עתיד / כאן

לאחר מכן: **תחום חיים** (8 תחומים) + "עם מי?" (אופציונלי, טקסט חופשי/תגיות) → **תוצאה**: גליף + הסבר קצר "למה" (אילו ערוצים הצביעו לאן) + CTA: "לעבור מכאן" (מוביל לכלי המתאים ב-`מעבר`) או "רק לרשום".

**ניקוד (`domain/checkin-scoring.ts`):** כל תשובה ממופה למשקלים {d3,d4,d5}; ערוץ הגוף במשקל ×1.5 ("הגוף הוא הערוץ הישר ביותר"). תוצאה = argmax; אם הפער בין שני הראשונים <15% — מוצג "בין 3D ל-4D". המשקלים בקובץ תוכן, לא בקוד.

**מצב מהיר:** לחיצה ארוכה על לשונית "בדיקה" → רישום בהקשה אחת (3 גליפים + תחום). למתקדמים.

### 6.4 אבחון מלא — "הדפוס הדומיננטי שלי"
מקור: `consciousness-model.md §5–6`. 12 שאלות, 5 תחומים; סדר התשובות מעורבב אקראית (לא תמיד א=3D); ללא חשיפת המפתח. שמירה אוטומטית באמצע.
**תוצאה:** אחוזים כלליים + **פרופיל לפי תחום** (5 פסים אופקיים מפולחים) + זיהוי הפער הגדול ביותר → הצעת **תחום מוקד** + נקודת כניסה למסע (שלב א'/ב'/ג').
**"שאל אדם קרוב":** יצירת קישור/טקסט לשיתוף עם 12 השאלות בגוף שלישי; הזנת תשובותיו ידנית; תצוגת השוואה עצמי-מול-אחר (שאלון עצמי מוטה כלפי מעלה — זה תיקון ההטיה).
חוזר בשבועות 0, 4, 8, 12 ואחר כך רבעונית; גרף מגמה בין מדידות.

### 6.5 מעבר — ארגז הכלים
מקור: `transitions.md`. שלוש כניסות:
- **"מאיפה אני בא"** — 3D→4D (6 כלים) · 4D→5D (7 כלים) · קיצורי דרך ישירים · "נפלתי".
- **לפי טריגר** — ביקורת, אי-ודאות, אי-צדק, דחייה, עומס, השוואה, פקק/תקלה.
- **לפי תחום חיים** — עבודה, כסף, זוגיות, משפחה, זמן, גוף.

כל כלי = כרטיס: שם · משך · "מתי להשתמש" · כפתור **התחל** → `Session Player`. אחרי כל סשן: "מה השתנה?" (גליף לפני/אחרי + הערה) — נשמר, ומזין את "מה עובד לי" בתובנות.
**המלצה חכמה:** בראש המסך — הכלי שהכי שיפר את המצב עבור הטריגר/התחום הזה בעבר.

### 6.6 Session Player — נגן התרגולים (לב טכני; משותף לשלב א' ו-ב')
תרגול = רצף **מקטעים** (`segments`) מקובץ תוכן:
`{ id, type: "instruction"|"breath"|"silence"|"prompt"|"bell", text, durationSec, breathPattern? }`
- **שלב א':** הטקסט מוצג גדול, מקטע אחר מקטע; טיימר; פעמון רך במעברים; עיגול נשימה מונפש לפי `breathPattern` (למשל 5-0-6-0); Wake Lock למניעת כיבוי מסך; מצב "עיניים עצומות" (מסך כהה, פעמונים בלבד, הקשה בכל מקום = השהה).
- **שלב ב':** אותו נגן בדיוק — אם קיים קובץ קול למקטע, הוא מנוגן במקום/לצד הטקסט. **לכן כבר בשלב א' כל טקסט הדרכה חייב להיות מפורק למקטעים עם `id` יציב.**
- רקע אופציונלי: pad אמביינטי רך מסונתז (Web Audio, שני אוסילטורים + LFO איטי). ללא טענות על "תדרים מרפאים".
- 12 תרגילי הליבה (ת1–ת12) + פרוטוקול 90 השניות + כלי המעבר — כולם רצים בנגן הזה.

### 6.7 מסע — תכנית 12 השבועות
מקור: `training-program.md`. שבוע 0 (מדידת בסיס) + שלושה שלבים: **קרקע** (1–3) · **המתבונן והצל** (4–8) · **לב, נוכחות, שירות** (9–12).
- תצוגת מסלול אנכי: שבועות כתחנות; השלב הנוכחי פתוח.
- מסך שבוע: תרגול יומי (קישור לנגן) · משימת החיים · סימון ביצוע יומי · רפלקציה שבועית קצרה.
- **קריטריוני מעבר** בין שלבים מוצגים כצ'קליסט רך — המשתמש מחליט; אין נעילה קשיחה. אפשר "להישאר עוד שבוע" בלי שזה ייראה ככישלון.
- נקודות בדיקה בשבועות 4/8/12: אבחון חוזר + מדדים מול שבוע 0.
- אזהרת שלב ב' ("זה השלב הסוער") מוצגת בכניסה לשבוע 4.
- **בלי streak-shaming:** יום שהוחמץ = נקודה ריקה, לא אדומה. חזרה אחרי הפסקה → "טוב שחזרת. 3 ימים של ת1–ת3 ואז ממשיכים."
- אחרי 12 שבועות: מצב **תחזוקה** (יומי מינימלי / שבועי / רבעוני) + תרגולים מתקדמים נפתחים בספרייה.

### 6.8 תזכורות (בלי שרת)
Push ב-iOS PWA דורש שרת — מחוץ לתחום. במקום זה: **ייצוא `.ics`** עם 5 עוגני הבדיקה היומיים (שעות מותאמות בהגדרות) ליומן של המכשיר, כולל deep link לבדיקה. `lib/ics.ts`.

### 6.9 תובנות
- **מפת חום:** יום בשבוע × חלק ביום, צבע = המצב השכיח + גליף קטן; סינון לפי תחום.
- **מפת תחומי החיים** (הטבלה מ-`identification.md §8`) — מתמלאת אוטומטית: מצב דומיננטי · טריגר עיקרי · "מה מחזיר אותי".
- **זמן התאוששות** לאורך זמן (מיומן הערב) — המדד המרכזי.
- **% בדיקות במצב 5D** לפי שבוע · **מה עובד לי** (כלים לפי שיפור ממוצע) · השוואת אבחונים.
- **דפוס אישי** מתוך השלושה בסקיל ("מקצוען-על" / "המחפש" / "הנזיר בבית") אם הנתונים תואמים — מנוסח בזהירות.
- מצב ריק: "עוד 12 בדיקות ותופיע כאן מפת החום שלך."

### 6.10 יומן
- **יומן ערב:** עד 3 אירועים טעונים: מה קרה → גוף → מה עשיתי → זמן עד איזון (דקות/שעות/ימים) → סיווג.
- **יומן טריגרים (ת5)** ו**חקירת אמונות (ת7)** כטפסים מודרכים רב-שלביים עם שמירה אוטומטית.
- **ערוץ השפה — ניתוח מקומי:** `domain/language-markers.ts` סופר בטקסט היומן סמני 3D ("חייב", "צריך", "אין ברירה", "תמיד", "אף פעם", "בגלל", "אין לי זמן") מול סמני 5D; מציג מגמה עדינה. regex מקומי בלבד, בלי AI, בלי שליחה לשום מקום.

### 6.11 ספרייה
המודל ומקורותיו · טבלת ההשוואה · מקבילות (מאסלו, קיגן, קבלה, מדעי המוח) · **חמשת ערוצי הזיהוי** · **"זיוף 5D"** (טבלה חשובה — נגישה גם מהתוצאה של בדיקה) · סימני התקדמות אמינים ולא אמינים · **נספח פיזיקה** (קוונטים, מרחב-זמן, מימד חמישי, חורי תולעת, מיתוסים קוונטיים) עם תגיות רובד · מקורות להעמקה. חיפוש טקסט חופשי.

### 6.12 "צריך עזרה?"
סימני האזהרה מהסקיל בלשון רכה; "תרגול בריא מגביר תפקוד"; המלצה לפנות לאיש מקצוע; **ער"ן — 1201** (חיוג ישיר). נגיש מההגדרות ומכל תוצאת בדיקה שבה כיווץ=10 שלוש פעמים ברצף (הצעה עדינה, לא חוסמת).

### 6.13 הגדרות וגיבוי
ערכת צבע (כהה/בהיר/מערכת) · גודל טקסט · צלילים ועוצמות · שעות העוגנים · תחום מוקד · **ייצוא/ייבוא JSON** של כל הנתונים · מחיקת הכול (עם אישור כפול) · שלב ב': שפת הדרכה, קול, עוצמת קול/רקע.

---

## 7. מודל הנתונים (Dexie)

```ts
type Dim = 'd3' | 'd4' | 'd5';
type Domain = 'work'|'money'|'couple'|'family'|'body'|'time'|'creation'|'alone';

interface CheckIn { id; ts: number; anchor?: AnchorId; answers: {...};
  scores: Record<Dim, number>; result: Dim | 'd3-d4' | 'd4-d5';
  domain: Domain; withWhom?: string[]; note?: string; quick: boolean }

interface Diagnosis { id; ts; by: 'self'|'other'; answers: Record<QId, Dim>;
  overall: Record<Dim, number>; byDomain: Record<DiagDomain, Record<Dim, number>>; weekMarker?: 0|4|8|12 }

interface SessionLog { id; ts; toolId: string; source: 'shift'|'journey'|'sos';
  before?: Dim; after?: Dim; trigger?: TriggerId; domain?: Domain; completed: boolean; note?: string }

interface EveningEntry { id; date; events: Array<{ what; body; did; recoveryMin: number; dim: Dim }> }
interface GuidedForm { id; ts; kind: 'trigger-journal'|'belief-inquiry'|'shadow-321'|'forgiveness'; fields: Record<string,string> }
interface JourneyState { startedAt; currentWeek: 0..12; focusDomain: Domain; days: Record<ISODate,{practice:boolean; lifeTask:boolean}>;
  weeklyReflections: Record<number,string>; mode: 'program'|'maintenance' }
interface Settings { theme; textScale; anchors: Record<AnchorId,string>; sound: {...}; voice?: {...}; onboarded: boolean }
```
גרסאות סכמה עם migrations מהיום הראשון. כל כתיבה מיידית (auto-save) + אינדיקטור "נשמר" עדין.

---

## 8. חבילת התוכן — אבן דרך 0 (לאישור לפני כל קוד UI)

המרת `content-source/*.md` ל-JSON מטיפוסי ב-`src/content/`:

| קובץ | תוכן | מקור |
|---|---|---|
| `model.json` | שלושת המצבים, טבלת השוואה, מקבילות | consciousness-model §2–4 |
| `diagnosis.json` | 12 שאלות + מיפוי תשובה→מצב + גרסת גוף שלישי | consciousness-model §5–6 |
| `checkin.json` | 5 צעדים, אפשרויות, **משקלים**, מילון רגשות | identification §2–7 |
| `channels.json` | חמשת הערוצים (טבלאות), סמני שפה | identification §2–6 |
| `fake5d.json` | טבלת זיוף 5D | identification §9 |
| `tools.json` | כל כלי המעבר, כ-`segments` | transitions §2–5, §8 |
| `triggers.json`, `domains.json` | פרוטוקולים | transitions §6–7 |
| `exercises.json` | ת1–ת12 + פרוטוקול 90 שניות, כ-`segments` | training-program §6–7 |
| `journey.json` | שבוע 0 + 12 שבועות, קריטריונים, מכשולים, תחזוקה | training-program |
| `library.json` | מאמרי ספרייה כולל נספח פיזיקה | physics + השאר |
| `safety.json` | גבולות, סימני אזהרה, משאבים | SKILL.md |

**כתיבת `segments`:** זו עבודת עריכה, לא העתקה. כל תרגיל נכתב כהנחיה מדוברת בגוף שני, משפטים קצרים, קצב איטי, עם מקטעי `silence` מפורשים. לכל מקטע `id` יציב (`t1.s03`). **הטקסט הזה הוא גם תסריט הקול של שלב ב'** — לכתוב אותו כך שיישמע טוב בקול: בלי סוגריים, בלי קיצורים, בלי סימנים.

**תוצר לאישור:** קבצי ה-JSON + `CONTENT-REVIEW.md` קריא (כל התרגילים כטקסט רציף) — דודו עובר ומאשר/מתקן **בתוך Claude Code** לפני שממשיכים.

---

## 9. מנוע האודיו (קריטי לאייפון)

`AudioEngine` יחיד (singleton) — כל צליל באפליקציה עובר דרכו:
1. `AudioContext` נוצר/מתחדש (`resume()`) **רק בתוך אירוע מגע** — כפתור "התחל" של כל סשן.
2. **עקיפת מתג השקט של iOS:** Web Audio מושתק כשהמכשיר על שקט, בעוד `<audio>` לא. בתחילת כל סשן מפעילים אלמנט `<audio>` בלולאה עם קובץ שקט קצר — זה מעביר את סשן האודיו ל-"playback" וגורם גם ל-Web Audio להישמע. (זה כנראה מקור התקלה באפליקציית Gateway.)
3. ערוצי mix נפרדים עם `GainNode`: `ui` (פעמונים) · `ambient` · `voice` (שלב ב'). סליידר לכל אחד.
4. קול (שלב ב') מנוגן דרך `fetch → decodeAudioData → BufferSource` באותו context — כך ה-mix אחיד ואין התנגשות בין `<audio>` ל-Web Audio.
5. `visibilitychange` / נעילת מסך: השהיה וחידוש נקיים. Wake Lock בזמן סשן.
6. **בדיקת קבלה חובה על iPhone פיזי ב-Safari וכ-PWA מותקן**, עם מתג שקט פועל וכבוי.

---

## 10. שלב ב' — הדרכה קולית מובנית (ElevenLabs)

> מתחיל רק אחרי ששלב א' מאושר. דודו יספק את מפתח ה-API בזמן הפיתוח. **המפתח נכנס ל-`.env` מקומי בלבד — לעולם לא לקוד, לא ל-git, לא ל-bundle.**

### 10.1 עיקרון: הפקה מראש, לא בזמן ריצה
האפליקציה סטטית וציבורית (GitHub Pages) — מפתח בצד לקוח היה נחשף. לכן: סקריפט Node מקומי מפיק קובצי MP3 לכל מקטע → נשמרים ב-`public/audio/voice/` → נפרסים כקבצים סטטיים. יתרונות: אפס עלות שוטפת, עובד offline, איכות אחידה, אפשר להאזין ולתקן לפני פרסום.

### 10.2 B0 — אודישן קולות (לפני הכול)
דודו ציין בעבר שקריינות עברית נשמעה רובוטית — הבחירה נעשית **באוזן, לא על הנייר**.
`tools/voice/audition.mjs`: מפיק את **אותן 3 פסקאות מבחן** (הנחיית נשימה איטית · הסבר קצר · משפט חם מתרגול הלב) ב-4–6 קולות מועמדים × המודלים הרלוונטיים → דף `audition.html` מקומי להשוואה. דודו בוחר קול אחד לעברית וקול אחד לאנגלית.
- **לבדוק בתיעוד העדכני של ElevenLabs** (בזמן הפיתוח) איזה מודל תומך הכי טוב בעברית כיום ומהם הפרמטרים שלו — לא להניח מראש. נקודת פתיחה מהסקיל `hebrew-tts-expert`: מודל רב-לשוני; לטון מדיטטיבי `stability` גבוה יחסית (~0.6–0.75), `style` נמוך, קצב איטי.
- לחפש בספריית הקולות קולות שתויגו להדרכה/מדיטציה/קריינות רגועה, ולכלול לפחות קול נשי אחד וגברי אחד.
- אם אף קול עברי אינו משביע רצון: חלופה מוסכמת — הדרכה באנגלית רכה + טקסט עברי על המסך (כפי שנעשה ב-Gateway).

### 10.3 B1 — תסריטי קול
`voice-scripts/{he,en}/*.json` נגזרים אוטומטית מ-`segments` שבתוכן (פרק 8), עם שכבת התאמה לקול:
```json
{ "sessionId": "t1", "lang": "he", "voiceId": "...", "model": "...", "settings": {...},
  "segments": [
    { "id": "t1.s01", "text": "שב בנוחות. תן לגוף להיות כבד.", "say": null, "pauseAfterSec": 4 },
    { "id": "t1.s02", "text": "שאיפה... חמש שניות.", "say": "שְׁאִיפָה... חמש שניות.", "pauseAfterSec": 0 }
  ] }
```
- **`say`** = כתיב חלופי להגייה בלבד (ניקוד חלקי / פירוק מילה) כשהמנוע טועה; `text` נשאר לתצוגה.
- `pronunciation.json` גלובלי: מילון תיקוני הגייה חוזרים (למשל "מֵמַד", "מודעוּת") — מוחל אוטומטית.
- **שתיקות אינן מופקות ב-TTS** — הנגן מבצע אותן. חוסך תווים ונותן שליטה מדויקת בקצב.
- תרגילי נשימה: הקול נותן פתיחה וסיום; **ספירת הנשימות נעשית בפעמונים/עיגול**, לא בקול חוזר (נשמע טבעי יותר וזול בהרבה).
- גרסת אנגלית: תרגום ועריכה של התסריטים לאנגלית רכה ופשוטה.

### 10.4 B2 — סקריפט ההפקה `tools/voice/generate-voice.mjs`
- קורא תסריטים → לכל מקטע מחשב **hash** של (טקסט להגייה + voiceId + model + settings).
- משווה ל-`public/audio/voice/manifest.json` → מפיק **רק מקטעים חדשים/ששונו** (אידמפוטנטי; תיקון מילה אחת = קריאה אחת).
- `--dry-run` / `estimate.mjs`: סופר תווים ומציג הערכת עלות **לפני** הרצה; דורש אישור מפורש (`--yes`).
- שומר `…/{lang}/{sessionId}/{segId}.mp3` (mono, bitrate מתון לקבצים קטנים) + מעדכן manifest: `{ file, hash, durationSec, chars }`.
- Rate limiting, retries עם backoff, המשך מנקודת עצירה, לוג שגיאות.
- `--only t1,t9` להפקה חלקית · `--lang he|en` · `--force`.
- בסיום: דף `voice-review.html` מקומי — האזנה למקטע מול הטקסט, סימון "להפיק מחדש".

**הערכת היקף:** ~12 תרגילי ליבה + ~20 כלי מעבר + 90 שניות + פתיחי שבועות ≈ 25–40 אלף תווים לשפה. הסקריפט יציג מספר מדויק ועלות לפי התוכנית של דודו לפני ההרצה.

### 10.5 B3 — שילוב בנגן
- `SegmentPlayer`: לכל מקטע — אם קיים ב-manifest קובץ לשפה הנבחרת → טעינה מוקדמת של המקטע הבא, ניגון בערוץ `voice`, ואז `pauseAfterSec`. אם אין קובץ → נפילה חלקה לטקסט+פעמון (האפליקציה לא נשברת אם אודיו חסר).
- Duck אוטומטי של ה-`ambient` (‎−6dB) בזמן דיבור, עם fade.
- הגדרות: שפת הדרכה (עברית/אנגלית/ללא) · עוצמת קול · עוצמת רקע · "הצג טקסט בזמן הדרכה".
- **Cache לפי דרישה:** הקול לא נכנס ל-precache של ה-PWA (משקל). נשמר ב-Cache Storage בפעם הראשונה שמנגנים סשן; כפתור "הורד את כל ההדרכות לשימוש offline" בהגדרות, עם חיווי נפח.
- מצב "עיניים עצומות" הופך לברירת המחדל כשיש קול.

### 10.6 B4 (אופציונלי, להחלטת דודו) — קול דינמי אישי, BYOK
משפטי פתיחה מותאמים ("בוקר טוב. אתמול זיהית 3D בשלוש ישיבות…") דורשים הפקה בזמן ריצה. אפשרי רק במודל BYOK: המפתח מוזן בהגדרות, נשמר ב-IndexedDB **במכשיר בלבד**, נשלח ישירות ל-ElevenLabs. יש להציג אזהרת אבטחה ברורה ולהמליץ על מפתח מוגבל-מכסה. **ברירת מחדל: כבוי. לא לבנות בלי אישור מפורש.**

### 10.7 קבלה לשלב ב'
קול נשמע באייפון במתג שקט פועל וכבוי · אין "קפיצות" בין מקטעים · mix מאוזן · תיקון מילה בתסריט מפיק מחדש מקטע אחד בלבד · המפתח אינו מופיע ב-`dist/` (בדיקת grep ב-CI) · האפליקציה תקינה לגמרי בלי תיקיית הקול.

---

## 11. איכות, בדיקות, נגישות

- **Vitest:** `checkin-scoring`, `diagnosis-scoring` (כולל ערבוב תשובות), `heatmap`, `journey` (מעברי שבוע, חזרה מהפסקה), `language-markers`, `backup` (ייצוא→ייבוא = זהות), ולידציית תוכן (כל `toolId`/`exerciseId` מקושר קיים; כל `segment.id` ייחודי).
- **Playwright:** זרימות מלאות (onboarding → בדיקה → מעבר → סשן → תובנות) ב-viewport iPhone 14 ודסקטופ; צילומי מסך RTL בכהה ובבהיר; בדיקת אין-גלילה-אופקית; `prefers-reduced-motion`.
- **נגישות:** axe ב-e2e; ניגודיות ≥4.5:1 לכל זוגות ה-tokens (בדיקה אוטומטית); ניווט מקלדת; VoiceOver קורא גליפים כ-"מצב 3D" וכו'.
- **ביצועים:** bundle ראשוני <250KB gzip; code-split לפי feature; Lighthouse PWA ≥95.
- **Offline:** כל שלב א' עובד במצב טיסה אחרי טעינה ראשונה.
- לפני כל מסירה: Pre-Delivery Checklist של `ui-ux-pro-max`.

---

## 12. אבני דרך — סדר העבודה המחייב

כל אבן דרך מסתיימת ב: בדיקות עוברות · commit · סיכום קצר לדודו · **המתנה לאישור** לפני הבאה.

### שלב א' — האפליקציה
| # | אבן דרך | תוצר | קבלה |
|---|---|---|---|
| **M0** | **חבילת תוכן** | JSON + סכמות Zod + `CONTENT-REVIEW.md` | דודו קרא ואישר את התוכן והתסריטים. **אין קוד UI לפני זה** |
| M1 | שלד ומערכת עיצוב | Vite/TS/Tailwind, tokens, primitives, גליפים, Shell+ניווט, עמוד `/#/design` עם כל הרכיבים | מאושר ויזואלית, כהה+בהיר, RTL |
| M2 | נתונים | Dexie, repositories, migrations, גיבוי | בדיקות יחידה; ייצוא/ייבוא |
| M3 | Onboarding + בדיקת מימד + "היום" | הזרימה המרכזית מקצה לקצה | בדיקה מלאה <75 שנ'; תוצאה מוסברת |
| M4 | AudioEngine + Session Player + ת1–ת12 + 90 שניות | נגן עובד עם טקסט, פעמונים, נשימה | **נבדק על iPhone פיזי** (פרק 9) |
| M5 | מעבר (ארגז כלים) | 3 כניסות, לפני/אחרי, המלצה | כל הכלים רצים בנגן |
| M6 | אבחון מלא + "שאל אדם קרוב" | שאלון, פרופיל, השוואה, מגמה | ניקוד נבדק ביחידה |
| M7 | מסע 12 שבועות | מסלול, שבועות, נקודות בדיקה, תחזוקה | מעברי שלב; חזרה מהפסקה |
| M8 | יומן + תובנות | יומן ערב, טפסים מודרכים, מפת חום, מדדים | מצבים ריקים; נתוני דמה לבדיקה |
| M9 | ספרייה, עזרה, הגדרות, `.ics` | — | חיפוש; ייצוא יומן עובד באייפון |
| M10 | PWA, offline, ליטוש, QA, פריסה | GitHub Pages + Actions | Lighthouse; e2e; checklist עיצוב |

### שלב ב' — קול
| # | אבן דרך | קבלה |
|---|---|---|
| B0 | אודישן קולות | דודו בחר קול עברי וקול אנגלי |
| B1 | תסריטים + מילון הגייה + גרסת אנגלית | דודו אישר טקסטים |
| B2 | סקריפט הפקה + הערכת עלות + הפקה + דף ביקורת | דודו האזין ואישר; hash-cache עובד |
| B3 | שילוב בנגן, mix, cache לפי דרישה, הגדרות | קבלה לפי 10.7 |
| B4 | (אופציונלי) קול דינמי BYOK | רק באישור מפורש |

**שלב ג' (רעיונות לעתיד, לא לבנות):** מאמן שיחתי ב-Anthropic API במודל BYOK המבוסס על הסקיל; ווידג'ט iOS דרך Shortcuts; אינטגרציית HRV.

---

## 13. הפרומפט הראשון ל-Claude Code

```
קרא את SPEC.md ואת CLAUDE.md במלואם, ואת כל הקבצים ב-content-source/.
בצע את אבן דרך M0 בלבד: בנה את חבילת התוכן (src/content/*.json + schema.ts)
ואת CONTENT-REVIEW.md. אל תכתוב קוד UI. כשתסיים — הצג לי סיכום ושאלות פתוחות,
והמתן לאישורי.
```
מומלץ לעבוד ב-Plan Mode בתחילת כל אבן דרך, ו-`/clear` בין אבני דרך כדי לשמור על context נקי.
